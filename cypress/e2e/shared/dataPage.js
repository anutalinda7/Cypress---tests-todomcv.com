    
    export const todoItemCat = "feed the cat"; 
    export const todoItemDog = "feed the dog"; 
    export const filterTexts = ['All', 'Active', 'Completed'];
    export const listOfItems = [`${todoItemCat} Red`, `${todoItemDog} Rex`, `${todoItemCat} Tom`, `${todoItemDog} Lightning`, 'feed the fish', 'feed the turtle']
    // shadow DOM tests
    export const namesOfBtnShadowWeb = ['File', 'Edit', 'View', 'Encoding'];
    